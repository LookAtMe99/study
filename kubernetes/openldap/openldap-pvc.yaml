apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-volume-openldap
  labels:
    type: local
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /tmp/data/openldap
  persistentVolumeReclaimPolicy: Recycle